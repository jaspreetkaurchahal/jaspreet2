function processJSON
{
var raw file= new XMLHttpRequest();
rawfile.open( "GET",'userdatData.json', false);
 
 var allText='';
 rawfile.onreadystatechannge= function ()
 {
 if (rawfile.readyState === 4)
 {
 if

 {
 if (rawfile.Status === 200)|| rawfile.status== 0)
 {
 allText= rawFile.resposneText;
 alert(allText);
 }
 }
 }
 rawFle.send(null);